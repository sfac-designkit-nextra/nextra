import Image from "next/image";

# Searchfield

Searchfield는 검색 기능을 구현할 때 사용하는 컴포넌트입니다.
검색값을 입력하고 ENTER 또는 돋보기 버튼을 눌렀을 때 동작하게 됩니다.

<br />
<div
  style={{
    border: "0.5px solid gray",
    borderRadius: "8px",
    display: "flex",
    justifyContent: "center",
    alignItems: "center",
    padding: "32px",
}}

>

  <div style={{
    display: "block"
  }}>
    <Image
      src="/Searchfield/Default.svg"
      alt="Searchfield"
      width={300}
      height={300}
    />
  </div>
  <div style={{
    display: "block"
  }}>
  <Image
    src="/Searchfield/Focused.svg"
    alt="Searchfield"
    width={300}
    height={300}
  />
  </div>
</div>

```tsx
<Searchfield />
```
